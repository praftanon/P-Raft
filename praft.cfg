CONSTANTS
  A = {A1, A2, A3}
  B = {B1, B2, B3}
  C = {C1, C2, C3}
  NoLeader = NoLeader

  MAXLEN = 1      \* 日志长度上限（可以先 3，跑通后再升）
  TMAX   = 2      \* 任期和值域上限
  REPL_BC = TRUE \* 先关闭 B/C 异步复制以加速；需要时设 TRUE

INIT Init
NEXT Next

CHECK_DEADLOCK FALSE

\* 同时宕机台数围栏（配合 Crash/Recover 一次 1 台，进一步收敛状态）
\* 可选：如果需要，加上：
\* CONSTRAINT Cardinality(Nodes \ Alive) <= 1

INVARIANTS
  Safety
  LeaderCompleteness
  CommittedOnAMajority
  BoundedCommit
